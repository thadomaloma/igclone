<!DOCTYPE html>
<html>
  <head>
    <title>IgClone</title>
    <meta name="viewport" content="width=device-width">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <div class="container">
      <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container-fluid">
        <div id="gnavi" class="collapse navbar-collapse">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li><%= link_to image_tag('logo.png'), posts_path %></li>
          </ul>
          <ul>
          <% if logged_in? %>
            <li class="btn btn-light"><%= link_to 'Post list', posts_path %></li>
          <% else %>
            <li class="btn btn-light"><%= link_to 'Post list', new_session_path %></li>
          <% end %>
          <% if logged_in? %>
            <li class="btn btn-light"><%= link_to 'Create New', new_post_path %></li>
            <li class="btn btn-light"><%= link_to "Profile", user_path(current_user.id) %></li>
            <li class="btn btn-light"><%= link_to "Logout", session_path(current_user.id), method: :delete, data: { confirm: 'Are you sure you want to log out?' } %></li>
          <% else %>
            <li class="btn btn-light"><%= link_to "Sign up", new_user_path %></li>
            <li class="btn btn-light"><%= link_to "Login", new_session_path %></li>
          <% end %>
          </ul>
        </div>
      </div>
    </nav>

    <div class = "main_content">
    <%= yield %>
    </div>

    <footer class="topic">
      <p>©2022 INSTAGRAM CLONE, DIVE INTO CODE.</p>
    </footer>
    </div>
  </body>
</html>
